{
  "name": "gc-configtrack",
  "version": "0.4.0",
  "description": "Projeto acadêmico para aplicação prática de Gerência de Configuração.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "verify": "npm test && npm run build && npm run validate:build",
    "validate:build": "node scripts/validate-build.js"
  },
  "engines": {
    "node": "24.18.0"
  }
}
